# Local dev: the Vite proxy forwards /api to http://localhost:8000, so leave blank.
# Production (Vercel): set this to your Railway backend URL, e.g. https://moviemine-production.up.railway.app
VITE_API_URL=
